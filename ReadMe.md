
# MkDocs run 

Run on Windows

```python
python -m mkdocs serve     
python -m  mkdocs gh-deploy
```

```
python3 -m venv .venv && source .venv/bin/activate && pip install --upgrade pip setuptools wheel && pip install mkdocs mkdocs-material && mkdocs --version
```

Mermaid plugin for diagrams

```
python -m pip install mkdocs-mermaid2-plugin
```